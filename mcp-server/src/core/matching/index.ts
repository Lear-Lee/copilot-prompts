/**
 * 匹配模块入口
 * 
 * 设计理念：MCP 只提供映射信息，不做复杂匹配
 * 复杂的意图分析和权重计算由 AI 自己完成
 */

// 注意：weights.ts, intentAnalyzer.ts, standardMatcher.ts 已被移除
// 这些功能过度设计了，AI 本身就擅长做这些事情
// 保留简洁的映射表供 AI 查询

export {}; // 占位，实际映射在 ../mappings/ 目录

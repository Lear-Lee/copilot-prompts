{
    "v1.0.0": ["需求解释：从现在起，当我提出《更新v x.x.x》时，需要将本文件内对应版本的规则应用到copilot-prompts本体MCP中。", "注：本文件（admin-rules.json），里面以键值对的方式（如：v1.0.0: [...]）记录了我需要更新copilot-prompts本体MCP的需求，属于对底层逻辑的强制更新", "本文件内的规则默认只对本目录下的mcp服务器生效，不会影响其他mcp服务器，优化开发也仅针对本mcp服务器。", "本文件内为copilot-prompts自身迭代更新，在优化时需要将对应版本的规则作为本mcp服务器的强制默认规则。", "每次优化后，需要同步更新setup-copilot.sh以确保能让用户使用最新优化的版本。"],
    "v1.1.0": {
        "status": "✅ 已完成 (2025-12-19)",
        "rules": ["代码更新后需要确保能够运行，不要出现任务执行完后用户代码都跑不起来的情况。", "优化代码时需要考虑到不同用户的使用场景，避免出现某些用户无法使用的情况。", "目前发现的重大错误且反复多次对话都无法修复的场景：代码语法错误（如</style></style>重复出现，缺少括号，生成代码导致老代码不全等）"],
        "implementations": [
            "✅ 新增 CodeValidator 核心模块 (src/core/codeValidator.ts)",
            "✅ 实现语法错误自动检测（重复标签、括号不匹配等）",
            "✅ 实现代码完整性检查",
            "✅ 实现自动修复机制",
            "✅ 集成到 generateConfig 工具",
            "✅ 更新版本号到 1.5.0",
            "✅ 更新 CHANGELOG.md",
            "✅ 更新 README.md",
            "✅ 更新 setup-copilot.sh",
            "✅ 编译测试通过"
        ],
        "next_steps": [
            "在实际使用中监控验证效果",
            "根据用户反馈优化验证规则",
            "考虑添加更多编程语言的语法检查"
        ]
    }
}
# Custom Agents 使用指南

Custom Agents 是 VS Code Copilot 的强大功能，允许您创建专门的 AI 代理，每个代理都遵循特定的开发规范。

## 🎯 为什么使用 Custom Agents？

### vs. Copilot Instructions (.github/copilot-instructions.md)

| 特性 | Copilot Instructions | Custom Agents |
|-----|---------------------|---------------|
| 作用范围 | 全局，所有对话 | 按需选择，精确控制 |
| 多规范支持 | 只能有一个活动文件 | 可同时使用多个 agents |
| 切换成本 | 需要修改文件或符号链接 | @agent-name 即可切换 |
| 团队协作 | 统一规范 | 灵活选择 |
| 最佳用途 | 项目基础规范 | 特定任务、领域规范 |

### 推荐组合使用

```
.github/
├── copilot-instructions.md    # 项目基础规范（如 VitaSage 专用）
└── agents/                     # 领域专用 agents
    ├── vue3.agent.md          # 通用 Vue 3 开发
    ├── typescript.agent.md    # 严格类型检查
    └── i18n.agent.md          # 国际化重构
```

**实践**:
- `copilot-instructions.md`: 项目整体架构和强制规范
- `agents/`: 特定任务的辅助规范

---

## 🚀 快速开始

### 1. 同步 Agents 到项目

```bash
cd /Users/pailasi/Work/copilot-prompts
./sync-agents.sh /path/to/your-project
```

### 2. 在 VS Code 中使用

打开 Copilot Chat (`Cmd+Shift+I`)，使用 `@` 调用 agent：

```
@vitasage 创建一个用户管理的 CRUD 页面

@vue3 生成一个可复用的搜索框组件

@typescript 为这个函数添加完整的类型定义

@i18n 重构这段代码，将所有硬编码文本改为 $t()
```

### 3. 验证 Agent 是否加载

```
# 在 Copilot Chat 中输入
@vitasage 你遵循哪些规范？

# 应该会列出 VitaSage 的核心原则
```

---

## 📋 可用的 Agents

### @vitasage - VitaSage 专用

**适用场景**:
- 创建符合 VitaSage 架构的组件
- CRUD 页面开发
- LogicFlow 流程图集成
- 表达式构建器

**核心规范**:
- API 调用必须使用 `api.$methodName`
- 所有文本必须国际化 `$t()`
- TypeScript 严格模式
- Element Plus 组件库

**示例**:
```
@vitasage 创建一个操作类(OperationClass)的管理页面，包括：
- 左侧：操作类列表（分页）
- 右侧：参数列表
- 支持新增、编辑、删除
- 所有文本国际化
```

---

### @vue3 - Vue 3 通用

**适用场景**:
- 创建可复用组件
- 标准表单处理
- 响应式数据管理
- Props/Emits 类型定义

**核心规范**:
- Composition API (`<script setup lang="ts">`)
- Props/Emits 类型化
- 响应式最佳实践

**示例**:
```
@vue3 创建一个日期范围选择器组件，要求：
- 支持 v-model
- 可选 disabled 状态
- emit change 事件
- 完整的 TypeScript 类型
```

---

### @typescript - TypeScript 严格

**适用场景**:
- API 响应类型定义
- 复杂数据结构建模
- 类型守卫编写
- 重构去除 any

**核心规范**:
- 零 any 类型
- 严格空检查
- 泛型正确使用
- 类型守卫

**示例**:
```
@typescript 为这个 API 响应定义完整类型，包括：
- 分页数据结构
- 错误处理
- 空值安全
```

---

### @i18n - 国际化

**适用场景**:
- 重构硬编码文本
- 创建多语言组件
- 表单验证国际化
- 确认对话框文本

**核心规范**:
- 所有文本使用 $t()
- 参数化动态内容
- 键名使用点分层级

**示例**:
```
@i18n 重构这个组件，将所有硬编码中文改为 $t()，
并提供对应的翻译键建议
```

---

## 🔄 更新与维护

### 更新所有 Agents

```bash
# 1. 更新 prompts 仓库
cd /Users/pailasi/Work/copilot-prompts
git pull origin main

# 2. 同步到项目
./sync-agents.sh /path/to/your-project

# 3. 重新加载 VS Code
# Cmd+Shift+P → Reload Window
```

### 添加新 Agent

1. 在 `copilot-prompts/agents/` 创建新的 `.agent.md` 文件
2. 更新 `sync-agents.sh` 脚本
3. 运行同步脚本

---

## 💡 最佳实践

### 1. Agent 组合使用

```
# 先用项目 agent 创建基础结构
@vitasage 创建用户管理页面

# 再用 typescript agent 完善类型
@typescript 为这个组件添加完整类型定义

# 最后用 i18n agent 国际化
@i18n 重构文本为国际化
```

### 2. 明确的上下文

```
# ❌ 模糊
@vue3 写个组件

# ✅ 清晰
@vue3 创建一个搜索框组件，要求：
- 支持防抖
- v-model 绑定
- 可配置占位符
- TypeScript 类型完整
```

### 3. 迭代改进

```
# 第一轮：基础功能
@vitasage 创建用户列表页面

# 第二轮：完善类型
@typescript 补充缺失的类型定义

# 第三轮：国际化
@i18n 所有文本改为 $t()
```

---

## 🐛 故障排查

### Agent 不可用？

1. 检查文件是否存在
   ```bash
   ls -la .github/agents/
   ```

2. 重新加载 VS Code
   ```
   Cmd+Shift+P → Reload Window
   ```

3. 检查 agent 文件格式
   - 必须以 `.agent.md` 结尾
   - 必须有 YAML frontmatter

### Agent 不遵守规范？

1. 检查 agent 文件是否引用了正确的 prompt
2. 在对话中明确引用规范
   ```
   @vitasage 严格按照 VitaSage 的 API 调用规范，
   使用 api.$getUserPageList 而不是 axios
   ```

---

## 📚 相关资源

- [VS Code Custom Agents 文档](https://code.visualstudio.com/docs/copilot/copilot-extensibility-overview)
- [Agent 文件格式规范](https://code.visualstudio.com/docs/copilot/copilot-chat)
- [中央 Prompts 仓库](https://github.com/ForLear/copilot-prompts)
